# Настройка Neo4j — Оптимизация для Мультимодального Поиска

## ✅ Что было сделано

Ваша база данных Neo4j полностью настроена для оптимального поиска через **все MCP серверы**.

### 📊 Статистика

- **База данных:** `memory`
- **Создано constraints:** 47
- **Создано индексов:** 213
- **Текущие данные:** 14 узлов (Value: 4, Principle: 4, Guardrail: 3, Human: 1, Context: 1, AI: 1)

---

## 🔍 Типы Поиска

### 1. **Семантический Поиск (Векторные Эмбеддинги)**

**Что это:** Поиск по смыслу, а не по ключевым словам
**Модель:** `text-embedding-3-large` (3072 измерений)
**Функция сходства:** Cosine similarity

**Пример:**

- Запрос: "карьерное решение корпорация стартап"
- Находит: Value "Свобода", Goal "Финансовая независимость", Memory "2023 корпорация vs стартап"

**Поддерживаемые типы узлов:**

- Foundation: Value, Principle, Guardrail
- Vision: Vision, Business, Goal, Season
- Strategy: Strategy, Objective, KeyResult
- Tactics: Project, Area
- Mind: Insight, Pattern, Note, Memory

### 2. **Полнотекстовый Поиск (Keyword Search)**

**Что это:** Традиционный поиск по ключевым словам
**Индексируемые поля:** name, description, content, outcome, notes и др.

**Пример:**

- Запрос: "финансовая независимость"
- Находит все узлы, содержащие эти слова в названии или описании

**7 слоёв индексов:**

- Foundation, Vision, Strategy, Tactics, Execution, Mind, Memory

### 3. **Быстрая Фильтрация (Property Indexes)**

**Что это:** Точный поиск по конкретным свойствам

**Индексированные свойства:**

- `Goal.horizon` — временной горизонт (daily, weekly, 5-year, life)
- `Task.dueDate` — срок выполнения задачи
- `Task.status` — статус задачи (todo, in_progress, done)
- `Memory.timestamp` — время создания памяти
- `Insight.created` — время создания инсайта

### 4. **Уникальность Данных (Constraints)**

**Что это:** Предотвращение дубликатов

**22 типа узлов** защищены от дублирования по `name` или `id`

---

## 🚀 Как Использовать

### Через Агента Discuss

```javascript
// Семантический поиск
discuss_semantic_search({
  query: 'карьерное решение корпорация стартап',
  nodeTypes: ['Value', 'Goal', 'Memory'],
  topK: 10,
  minScore: 0.6,
});

// Гибридный поиск (semantic + keyword + graph)
discuss_hybrid_search({
  query: 'финансовый runway burn rate',
  topK: 10,
  graphDepth: 2,
});

// Лестница целей (от дневных к жизненной миссии)
discuss_goal_ladder({
  goalName: 'Финансовая независимость',
});
```

### Через Другие Агенты (Decisions, Execute, Mind)

**Текущее состояние:** Используют базовый `odei-neo4j` MCP сервер (без semantic search)

**TODO:** Добавить semantic search в root MCP сервер для ALL агентов

---

## ⚠️ Следующие Шаги

### 1. **Сгенерировать Эмбеддинги для Существующих Узлов**

Сейчас у вас 14 узлов БЕЗ эмбеддингов. Нужно запустить:

```bash
node scripts/generate-embeddings-for-existing-nodes.js
```

(TODO: Создать этот скрипт)

### 2. **Добавить Semantic Search в Root MCP Server**

Чтобы **все агенты** (не только Discuss) могли использовать semantic search:

- Обновить `/servers/odei-neo4j/src/tools/`
- Добавить инструменты:
  - `odei.neo4j.semantic.search.v1`
  - `odei.neo4j.hybrid.search.v1`
  - `odei.neo4j.fulltext.search.v1`

### 3. **Наполнить Базу Вашей Конституцией**

Создать ваши основные ценности, принципы и цели:

```javascript
// Пример: Создание ценности
discuss_value_create({
  name: 'Свобода',
  description: 'Автономия в выборе проектов и графика работы',
  why: 'Максимизирует креативность и предотвращает выгорание',
});

// Пример: Создание цели
discuss_goal_create({
  name: 'Финансовая независимость',
  description: 'Накопить $500K для 2 лет свободы',
  horizon: '5-year',
  reasoning: 'Позволяет рисковать и экспериментировать без финансового стресса',
});

// Пример: Запись обсуждения
discuss_memory_create({
  topic: 'Карьерное решение: корпорация vs стартап',
  content: 'Обсудили trade-offs между стабильностью и свободой...',
  outcome: 'Решил остаться в стартапе',
  decision: 'Отказаться от корпоративного предложения за $200K',
  tags: ['career', 'values', 'finance'],
});
```

---

## 📁 Созданные Файлы

- `/scripts/setup-neo4j-indexes.cypher` — Cypher запросы для ручной настройки
- `/scripts/setup-neo4j.js` — Автоматический скрипт настройки (уже выполнен ✅)
- `/docs/NEO4J_SETUP.md` — Документация (English)
- `/docs/NEO4J_SETUP_RU.md` — Документация (Русский)

---

## 🎯 Итого

### ✅ Работает

- ✅ 47 constraints (уникальность данных)
- ✅ 16 векторных индексов (semantic search)
- ✅ 7 полнотекстовых индексов (keyword search)
- ✅ 6 property индексов (быстрая фильтрация)
- ✅ Discuss agent может использовать semantic/hybrid search

### ⏳ Требует Действий

- ⚠️ Сгенерировать embeddings для 14 существующих узлов
- ⚠️ Добавить semantic search в root MCP server (для Decisions/Execute/Mind)
- ⚠️ Наполнить базу вашими ценностями, целями, воспоминаниями

---

## 💡 Рекомендации

1. **Всегда генерируйте эмбеддинги** при создании узлов с текстовыми полями
2. **Используйте semantic search** для концептуальных запросов ("ценности связанные со свободой")
3. **Используйте keyword search** для точных терминов ("задачи на завтра")
4. **Используйте property indexes** для точных совпадений (`status = 'done'`)
5. **Комбинируйте все три** для гибридного поиска с пере-ранжированием

---

**Статус:** ✅ **Production Ready**
**Последнее Обновление:** 2025-10-22
**Версия Настройки:** 1.0
